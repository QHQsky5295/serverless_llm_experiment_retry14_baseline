# dLoRA Gate: 2026-05-19

Status: closed as gate-only, not a formal-table row.

The first gate checks whether the upstream artifact can consume the closed
true-remote workload without changing the workload variables:

- same shared trace;
- same 500-adapter subset;
- same Llama-2 7B and Llama-3.2 3B model targets;
- real PEFT adapter files, not dummy/random LoRA tensors;
- `e2e_v3` replay and summary if runtime support exists.

Evidence is written under:

```text
/home/qhq/serverless_llm_baselines/results/paper_experiments/15_new_serverless_baselines_remote_v1/gates/dlora/
```

## Result

Source gate:

- closed true-remote inputs are valid: 4000 requests and 500 adapters;
- Llama-2 7B source support is present;
- Llama-3.2 3B support is not present in the artifact source;
- real PEFT `adapter_model.safetensors` loading for the closed adapter set is
  not exposed;
- no native `e2e_v3` replay path is present.

Build/import gate:

- isolated environment: `dlora_medusa_clone_20260519`;
- Python: 3.9.25;
- PyTorch: 2.1.2+cu121;
- NVCC: 12.1.105;
- local compatibility patch:
  `../patches/modern_ray_import_compat.patch`;
- import output: `vllm.__version__ == 0.1.4`.

Decision: do not launch the long formal replay. The artifact now builds and
imports on this machine, but running it with dLoRA's dummy/random LoRA tensors
would not be a valid comparison against the closed real-remote LoRA workload.
It can be cited only as a reproduction-gated candidate unless a real PEFT
adapter loader, Llama-3.2 path, and `e2e_v3` replay wrapper are added and
validated without changing the scheduling algorithm.

Tracked summary evidence:

```text
DLoRA_project/evidence/gate_2026-05-19.json
```
