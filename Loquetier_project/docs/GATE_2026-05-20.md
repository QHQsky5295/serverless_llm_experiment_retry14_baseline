# Loquetier Gate Notes 2026-05-20

Initial adaptation gates are in progress.  The first accepted boundary is:

- isolate Loquetier in `loquetier_20260520`;
- compile upstream SMLM kernels for RTX 3090 (`sm_86`);
- add local build support for Llama-3.2 3B's GQA group size 3;
- add PEFT/Transformers compatibility shims only where upstream APIs moved;
- run real Llama-3.2 3B and Llama-2 7B adapter smoke tests before any formal run.

This does not change Loquetier's virtualized multi-LoRA forward path or replace
its scheduler with PrimeLoRA/FaaSLoRA logic.

## Closed Gate Update

The first real-adapter gate is closed:

- Llama-3.2 3B, first 2 adapters, 16 filtered trace requests:
  `ok=16/16`, result sha256
  `6538c487e8c4c004a7339d4fcf0ee5e37b6dd8fde787a4c8623c26e37e702bd5`.
- Llama-2 7B, first 2 adapters, 16 filtered trace requests:
  `ok=16/16`, result sha256
  `438d4d6e9ae2695bf2361bd62b591a523ee635f7c429ee5806e40399f1b3dc22`.
- Llama-3.2 3B, first 16 adapters, 64 filtered trace requests:
  `ok=64/64`, result sha256
  `ad51908afe273241750bfe8e92b31ceeb4863c2ca9ca51ed976ce8fc860b2e05`.
- Llama-2 7B, first 16 adapters, 64 filtered trace requests:
  `ok=64/64`, result sha256
  `bdea361409c892604b8b16d4ed575dfc0f91fb93a67e41b68945a98f2c7f4ef2`.
- Llama-3.2 3B, first 64 adapters, 256 filtered trace requests:
  `ok=256/256`, result sha256
  `ee4ea8511603609adb7c6961e22702e280213784e75d20adff165122d517618b`.
- Llama-2 7B, first 64 adapters, 128 filtered trace requests:
  `ok=128/128`, result sha256
  `80d2378de2e69a8ac6c5ff7340a774df4cb39752fd5d37d609d5f3e79de83706`.

The compatibility patch is
`Loquetier_project/patches/loquetier_local_compat_20260520.patch`.
It records Python 3.9 annotation compatibility, PEFT active-adapter and import
compatibility, Llama-3.2 group-size-3 kernel build setup, and mixed-rank
adapter handling based on actual LoRA tensor shapes.  The patch does not
replace Loquetier's SMLM kernels or mixed-LoRA serving path.

Formal-table status: not adopted yet.  The current evidence proves Loquetier
can consume the closed real PEFT adapters and both backbone traces through 64
adapters, but the full 4000-request / 500-adapter workload is still untested.
