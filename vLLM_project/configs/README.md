# vLLM Project Configs

Standalone vLLM fair-comparison runs reuse the authoritative experiment config
from `/home/qhq/serverless_llm_experiment_retry14_baseline/configs/experiments.yaml`.

This directory is intentionally kept as a project-local placeholder. Do not add
system-specific workload, model, or LoRA sampling config here unless the same
configuration can be traced back to the shared fair-comparison artifacts.
