diff --git a/csrc/cuda_graph.cu b/csrc/cuda_graph.cu
index 450c826..fcdc7cb 100644
--- a/csrc/cuda_graph.cu
+++ b/csrc/cuda_graph.cu
@@ -128,7 +128,11 @@ void save_cuda_graph (
   CUgraphNode from[MAX_NODE_AND_EDGE_NUM];
   CUgraphNode to[MAX_NODE_AND_EDGE_NUM];
   size_t savedEdges = MAX_NODE_AND_EDGE_NUM;
+  #if defined(CUDA_VERSION) && CUDA_VERSION >= 12030
+  CU_CALL(cuGraphGetEdges(graph_, from, to, nullptr, &savedEdges));
+  #else
   CU_CALL(cuGraphGetEdges(graph_, from, to, &savedEdges));
+  #endif

   int saved_from_idx[MAX_NODE_AND_EDGE_NUM];
   int saved_to_idx[MAX_NODE_AND_EDGE_NUM];
diff --git a/csrc/spdk_util.cpp b/csrc/spdk_util.cpp
index bf42a07..c241a03 100644
--- a/csrc/spdk_util.cpp
+++ b/csrc/spdk_util.cpp
@@ -8,6 +8,7 @@
 #include <spdk/event.h>
 #include "util.h"

+#include <string>
 #include <vector>

 int node_idx;
@@ -150,6 +151,10 @@ void spdk_init(int num_disk)
     assert(THREAD_NUM == 4);
     opts.core_mask = "[0,1,2,3,4]";
     opts.mem_size = THREAD_NUM * 1024;
+    const char* no_huge = std::getenv("MEDUSA_SPDK_NO_HUGE");
+    if (no_huge != nullptr && std::string(no_huge) == "1") {
+        opts.no_huge = true;
+    }
     spdk_nvme_trid_populate_transport(&g_trid, SPDK_NVME_TRANSPORT_PCIE);
     snprintf(g_trid.subnqn, sizeof(g_trid.subnqn), "%s", SPDK_NVMF_DISCOVERY_NQN);
     if (spdk_env_init(&opts) < 0) {
@@ -307,4 +312,4 @@ void spdk_print_stat()
     total_io_size = 0;
     total_io_time = 0;
     cpu_cycle = 0;
-}
\ No newline at end of file
+}
diff --git a/setup.py b/setup.py
index 47522fa..063c8c8 100644
--- a/setup.py
+++ b/setup.py
@@ -46,9 +46,29 @@ def _is_cuda() -> bool:


 # Compiler flags.
-CXX_FLAGS = ["-g", "-O2", "-std=c++17", "-I/home/zsx/spdk/build/include"]
+MEDUSA_SPDK_ROOT = os.getenv("MEDUSA_SPDK_ROOT", "/home/zsx/spdk")
+MEDUSA_GDRCOPY_ROOT = os.getenv("MEDUSA_GDRCOPY_ROOT", "/usr/local")
+
+
+def _split_paths(value: str) -> List[str]:
+    return [p for p in value.split(os.pathsep) if p]
+
+
+MEDUSA_EXTRA_INCLUDE_DIRS = _split_paths(
+    os.getenv("MEDUSA_EXTRA_INCLUDE_DIRS", ""))
+MEDUSA_EXTRA_LIB_DIRS = _split_paths(os.getenv("MEDUSA_EXTRA_LIB_DIRS", ""))
+
+MEDUSA_INCLUDE_FLAGS = [
+    f"-I{MEDUSA_SPDK_ROOT}/build/include",
+    f"-I{MEDUSA_SPDK_ROOT}/dpdk/build/include",
+    f"-I{MEDUSA_GDRCOPY_ROOT}/include",
+]
+for include_dir in MEDUSA_EXTRA_INCLUDE_DIRS:
+    MEDUSA_INCLUDE_FLAGS.append(f"-I{include_dir}")
+
+CXX_FLAGS = ["-g", "-O2", "-std=c++17", *MEDUSA_INCLUDE_FLAGS]
 # TODO(woosuk): Should we use -O3?
-NVCC_FLAGS = ["-O2", "-std=c++17"]
+NVCC_FLAGS = ["-O2", "-std=c++17", *MEDUSA_INCLUDE_FLAGS]

 if _is_hip():
     if ROCM_HOME is None:
@@ -360,18 +380,66 @@ if _is_cuda():
         ))

 if not _is_neuron():
-    LDFLAGS1 = "-L/usr/local/lib/ -lgdrapi -Wl,--whole-archive -Wl,--no-as-needed -lspdk_sock_posix -lspdk_nvme -lspdk_sock -lspdk_trace -lspdk_rpc -lspdk_jsonrpc -lspdk_json -lspdk_vfio_user -lspdk_vmd -lspdk_util -lspdk_log -Wl,--no-whole-archive /home/zsx/spdk/build/lib/libspdk_env_dpdk.so -Wl,--no-as-needed /home/zsx/spdk/dpdk/build/lib/librte_bus_pci.so /home/zsx/spdk/dpdk/build/lib/librte_cryptodev.so /home/zsx/spdk/dpdk/build/lib/librte_dmadev.so /home/zsx/spdk/dpdk/build/lib/librte_eal.so /home/zsx/spdk/dpdk/build/lib/librte_ethdev.so /home/zsx/spdk/dpdk/build/lib/librte_hash.so /home/zsx/spdk/dpdk/build/lib/librte_kvargs.so /home/zsx/spdk/dpdk/build/lib/librte_log.so /home/zsx/spdk/dpdk/build/lib/librte_mbuf.so /home/zsx/spdk/dpdk/build/lib/librte_mempool.so /home/zsx/spdk/dpdk/build/lib/librte_mempool_ring.so /home/zsx/spdk/dpdk/build/lib/librte_net.so /home/zsx/spdk/dpdk/build/lib/librte_pci.so /home/zsx/spdk/dpdk/build/lib/librte_power.so /home/zsx/spdk/dpdk/build/lib/librte_rcu.so /home/zsx/spdk/dpdk/build/lib/librte_ring.so /home/zsx/spdk/dpdk/build/lib/librte_telemetry.so /home/zsx/spdk/dpdk/build/lib/librte_vhost.so -Wl,-as-needed -Wl,-rpath=/home/zsx/spdk/dpdk/build/lib -L/home/zsx/spdk/isa-l/.libs -lisal -L/home/zsx/spdk/isa-l-crypto/.libs -lisal_crypto -pthread -lrt -luuid -lssl -lcrypto -lm -lfuse3 -laio -laio  -lcudart -lcublas -lgdrapi"
-    flags = LDFLAGS1.split()
-    new_cxx_flags = flags.extend(CXX_FLAGS)
+    spdk_lib_dir = f"{MEDUSA_SPDK_ROOT}/build/lib"
+    dpdk_lib_dir = f"{MEDUSA_SPDK_ROOT}/dpdk/build/lib"
+    isal_lib_dir = f"{MEDUSA_SPDK_ROOT}/isa-l/.libs"
+    isal_crypto_lib_dir = f"{MEDUSA_SPDK_ROOT}/isa-l-crypto/.libs"
+    gdrcopy_lib_dir = f"{MEDUSA_GDRCOPY_ROOT}/lib"
+    dpdk_libs = [
+        "bus_pci", "cryptodev", "dmadev", "eal", "ethdev", "hash",
+        "kvargs", "log", "mbuf", "mempool", "mempool_ring", "net", "pci",
+        "power", "rcu", "ring", "telemetry", "vhost"
+    ]
+    link_dirs = [
+        gdrcopy_lib_dir, spdk_lib_dir, dpdk_lib_dir, isal_lib_dir,
+        isal_crypto_lib_dir, *MEDUSA_EXTRA_LIB_DIRS
+    ]
+    LDFLAGS1 = [
+        *(f"-L{path}" for path in link_dirs),
+        *(f"-Wl,-rpath={path}" for path in link_dirs),
+        "-lgdrapi",
+        "-Wl,--whole-archive",
+        "-Wl,--no-as-needed",
+        "-lspdk_sock_posix",
+        "-lspdk_nvme",
+        "-lspdk_sock",
+        "-lspdk_trace",
+        "-lspdk_rpc",
+        "-lspdk_jsonrpc",
+        "-lspdk_json",
+        "-lspdk_vfio_user",
+        "-lspdk_vmd",
+        "-lspdk_util",
+        "-lspdk_log",
+        "-Wl,--no-whole-archive",
+        f"{spdk_lib_dir}/libspdk_env_dpdk.so",
+        "-Wl,--no-as-needed",
+        *(f"{dpdk_lib_dir}/librte_{lib}.so" for lib in dpdk_libs),
+        "-Wl,-as-needed",
+        "-lisal",
+        "-lisal_crypto",
+        "-pthread",
+        "-lrt",
+        "-luuid",
+        "-lssl",
+        "-lcrypto",
+        "-lm",
+        "-lfuse3",
+        "-laio",
+        "-laio",
+        "-lcudart",
+        "-lcublas",
+        "-lgdrapi",
+    ]
     vllm_extension = CUDAExtension(
         name="vllm._C",
         sources=vllm_extension_sources,
         extra_compile_args={
-            "cxx": flags,
+            "cxx": CXX_FLAGS,
             "nvcc": NVCC_FLAGS,
         },
         libraries=["cuda"] if _is_cuda() else [],
-        extra_link_args = LDFLAGS1.split()
+        extra_link_args=LDFLAGS1
     )
     ext_modules.append(vllm_extension)

